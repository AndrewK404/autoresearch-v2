# LESSONS

Confirmed lessons grounded in own experience.

In research-only mode, a finding is promoted here only after the user
explicitly confirms it as solid (per `CONFIG.md ## Done criterion`). The
lesson must cite the supporting `log/NNN-*.md` entries that contain the
underlying thoughts, conclusions, and falsification record.

(no lessons yet)
